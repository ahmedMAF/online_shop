<%- include("./part/start.ejs") %>
    <style>
        .form-control:focus{
            box-shadow: none;
            border: 1px solid var(--bs-primary);
            background: var(--bs-border-color);
        }
        .card{
            border: 1px solid var(--bs-primary);
        }
        @media(min-width: 1000px){
            .card{
                width: 330px !important;
            }
        }
    </style>
</head>
<body>
<%- include("./part/nav-bar.ejs") %>

<div class="container mt-5">
    <div class="row pt-5">
        <div class="col-12 col-sm-6">
            <div class="card mb-4" style="width: 100%;">
                <img src="/<%= product.image %>" class="card-img-top img-fluid" alt="">
                <div class="card-body">
                    <h5 class="card-title"> <%= product.name %> </h5>
                    <form action="/cart" method="POST">
                        <input type="hidden" name="name" value="<%= product.name %>">
                        <input type="hidden" name="price" value="<%= product.price %>">
                        <input type="number" name="amount" placeholder="الكمية" class="mt-3 form-control"
                        style="border: 1px solid var(--bs-primary);">
                        <input type="hidden" name="idd" value="<%= product._id %>">
                        <input type="submit" value="إضافة إلي السلة" class="btn btn-primary mt-3">
                        <% if(typeof erorrAomunt !== "undefined") {%>
                            <p class="alert alert-primary text-center mt-3"> <%= erorrAomunt %> </p>
                        <%}%>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-12 col-sm-6" style="font-size: 20px; text-align: end;">
            <h1 class="mb-3"> <span class="text-primary"> الاسم: </span> <%= product.name %></h1>
            <p class="mb-3"><span class="text-primary">السعر:</span>$<%= product.price %></p>
            <p class="mb-4"> <span class="text-primary"> :وصف المنتج </span><br> <%= product.description %></p>
            <p> 
                يمكنك طلب المنتج علي واتساب أو على الأنستجرام أو على فيسبوك 
                كل ما عليك فعله هو أن تضغط على أحد الأيقونات التالية وتتواصل مع فريق الدعم لطلب المنتج
            </p>
            <ul style="font-size: 30px;" class="px-0 text-center">
                <a href="https://instagram.com/motarzat.ps.al3almea?igshid=YmMyMTA2M2Y=">
                    <i class="fa-brands fa-instagram ms-3 text-primary"></i></a>
                <a href="https://www.facebook.com/International.PS.Embroideries/">
                    <i class="fa-brands fa-facebook ms-3 text-primary"></i></a>
                <a href="https://wa.me/+972595831862"><i class="fa-brands fa-whatsapp ms-3 text-primary"></i></a>
            </ul>
        </div>
    </div>
</div>
<%- include("./part/end.ejs") %>